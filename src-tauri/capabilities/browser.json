{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "browser-window",
  "description": "Capability for the in-app browser window",
  "windows": ["browser"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
